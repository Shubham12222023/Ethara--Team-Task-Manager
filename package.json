{
  "name": "projectmaster-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
